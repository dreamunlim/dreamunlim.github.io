'use strict'

class GameState {
    constructor() {
        this.level = new Level(); // current game level
        // this.textureIDs = new Array(); // bookkeep the state's loaded textures
    }
  
}