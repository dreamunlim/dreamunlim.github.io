'use strict'

class Booster extends Enemy {
    constructor () {
        super();
        
    }

    initObject(initData) {
        super.initObject(initData);
    }

    updateObject() {
        super.updateObject();
    }


    drawObject() {
        super.drawObject();
    }
}